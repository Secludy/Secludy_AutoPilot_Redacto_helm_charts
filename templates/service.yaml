apiVersion: v1
kind: Service
metadata:
  name: {{ include "secludy.fullname" . }}
  labels:
    {{- include "secludy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.monitoring.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "secludy.selectorLabels" . | nindent 4 }}